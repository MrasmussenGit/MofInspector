<Window x:Class="MofInspector.CompareDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rule Detail Comparison"
        Height="650"
        Width="1000"
        Background="LightBlue"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Rule:" FontWeight="Bold" Margin="0,0,5,0"/>
            <TextBlock Name="RuleIdText" FontWeight="Bold"/>
            <TextBlock Text="  Status:" FontWeight="Bold" Margin="15,0,5,0"/>
            <TextBlock Name="OverallStatusText" FontWeight="Bold"/>
        </StackPanel>

        <ListView Name="PropertyDiffList"
                  Grid.Row="1"
                  SelectionMode="Extended"
                  Background="White"
                  KeyDown="PropertyDiffList_KeyDown">
            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <EventSetter Event="Click" Handler="GridViewColumnHeader_Click"/>
                </Style>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="Same">
                            <Setter Property="Background" Value="LightGreen"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="Different">
                            <Setter Property="Background" Value="LightCoral"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="Missing in File 1">
                            <Setter Property="Background" Value="LightSalmon"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="Missing in File 2">
                            <Setter Property="Background" Value="LightSalmon"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="Parsing Error">
                            <Setter Property="Background" Value="#FFF6D5"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="Info">
                            <Setter Property="Background" Value="#FFEFD5"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <!-- Removed Tag properties (GridViewColumn does not have Tag) -->
                    <GridViewColumn Header="Property" Width="220" DisplayMemberBinding="{Binding Key}"/>
                    <GridViewColumn Header="File 1 Value" Width="300" DisplayMemberBinding="{Binding Value1}"/>
                    <GridViewColumn Header="File 2 Value" Width="300" DisplayMemberBinding="{Binding Value2}"/>
                    <GridViewColumn Header="Status" Width="140" DisplayMemberBinding="{Binding Status}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,10,0,5">
            <CheckBox Name="ShowRawDiffCheckBox"
                      Content="Show Raw Text Diff"
                      Checked="ShowRawDiffChanged"
                      Unchecked="ShowRawDiffChanged"/>
            <CheckBox Name="ShowOnlyChangedLinesCheckBox"
                      Content="Only changed lines"
                      Margin="15,0,0,0"
                      Checked="ShowRawDiffChanged"
                      Unchecked="ShowRawDiffChanged"/>
            <TextBlock Text="(Scroll; changed lines highlighted; Ctrl+C copies selection)"
                       Margin="10,0,0,0"
                       Foreground="DimGray"
                       FontSize="11"/>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="File 1 Raw" Margin="0,0,5,0" FontWeight="Bold">
                <ListBox Name="RawLines1"
                         SelectionMode="Extended"
                         KeyDown="RawLines_KeyDown"
                         VirtualizingStackPanel.IsVirtualizing="True"
                         ScrollViewer.CanContentScroll="True">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}"
                                       Background="{Binding Bg}"
                                       FontFamily="Consolas"
                                       FontSize="12"
                                       TextWrapping="NoWrap"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Copy Selected Lines" Click="CopyRawLines_Click"/>
                            <MenuItem Header="Copy All Lines" Click="CopyAllRawLines_Click"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>
            </GroupBox>

            <GroupBox Header="File 2 Raw" Margin="5,0,0,0" FontWeight="Bold" Grid.Column="1">
                <ListBox Name="RawLines2"
                         SelectionMode="Extended"
                         KeyDown="RawLines_KeyDown"
                         VirtualizingStackPanel.IsVirtualizing="True"
                         ScrollViewer.CanContentScroll="True">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}"
                                       Background="{Binding Bg}"
                                       FontFamily="Consolas"
                                       FontSize="12"
                                       TextWrapping="NoWrap"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Copy Selected Lines" Click="CopyRawLines_Click"/>
                            <MenuItem Header="Copy All Lines" Click="CopyAllRawLines_Click"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>
            </GroupBox>
        </Grid>

        <DockPanel Grid.Row="4" Margin="0,10,0,0">
            <Button Content="Close"
                    Width="100"
                    Height="30"
                    DockPanel.Dock="Right"
                    HorizontalAlignment="Right"
                    Click="Close_Click"/>
        </DockPanel>
    </Grid>
</Window>